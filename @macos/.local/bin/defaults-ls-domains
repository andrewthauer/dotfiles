#!/usr/bin/env bash
#
# Pretty prints the macos defaults domains
#
# usage:
#   defaults-ls-domains
#   defaults-ls-domains | grep com.apple
#

defaults domains | awk '{gsub(", ", "\n", $0)}1' | sort
