#
# gcloud aliases
#
# - https://cloud.google.com/sdk/gcloud/
#

if ! command_exists "gcloud"; then
  return
fi

# alias gc-config-ls="gcloud config list"
# alias gc-compute-ls="gcloud compute instances list"
# alias gc-chg-clusters="gcloud container clusters get-credentials"
