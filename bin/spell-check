#!/usr/bin/env bash
#
# description:
#   Spell checks all files matching a glob pattern
#
# usage:
#   spell-check **/*.md
#

set -e

if [[ ! -x $(command -v aspell) ]]; then
  echo "aspell is not installed!"
  exit 1
fi

for file in $1; do
  aspell check "${file}";
done

exit 0
