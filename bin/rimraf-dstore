#!/usr/bin/env bash

if [[ "$OSTYPE" != darwin* ]]; then
  return
fi

run() {
  local search_path=$1
  if [ -v $1 ]; then
    search_path="."
  fi
  find "${search_path}" -iname ".DS_Store" -delete
}

run
