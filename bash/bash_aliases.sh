#!/usr/bin/env sh

# directories
alias cd..="cd .."
alias ll="ls -lhA"
alias lt="ls -ltA"
alias lsl="ls -lhA | ${PAGER}"
alias mkdir="mkdir -p"

# find here
alias fhere="find . -name"

# process
alias psg="ps aux | grep -v grep | grep -i"
