#!/usr/bin/env sh
# Executes commands at the start of an interactive session

# If not running interactively (powershell), don't do anything
[ -z "$PS1" ] && return

# Launch Zsh
if [ -t 1 ]; then
  exec zsh
fi

# Dotfiles directory
export DOTFILES="$HOME/.dotfiles"

# rbenv support
[ -s "${DOTFILES}/ruby/rbenv.zsh" ] && source "${DOTFILES}/ruby/rbenv.zsh"

# Alias definitions.
if [ -f "${DOTFILES}/bash/bash_aliases.sh" ]; then
  source ${DOTFILES}/bash/bash_aliases.sh
fi
